name: Test custom inputs - tool_name and level
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a valid workflow file for actionlint to lint
        run: |
          mkdir -p .github/workflows
          cat > .github/workflows/valid.yml << 'EOF'
          name: Valid Workflow
          on:
            push:
          jobs:
            check:
              runs-on: ubuntu-latest
              steps:
                - name: Hello
                  run: echo "world"
          EOF

      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          tool_name: my-actionlint
          level: warning
          reporter: local
          filter_mode: nofilter
          fail_level: none
        continue-on-error: true

      - name: Assert action ran with custom inputs
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran with custom tool_name and level (outcome=$OUTCOME)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
